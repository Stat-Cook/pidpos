% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_to_redaction_rules.R
\name{report_to_redaction_rules}
\alias{report_to_redaction_rules}
\title{Initialize redaction rules}
\usage{
report_to_redaction_rules(report, path = NULL, include_context = FALSE)
}
\arguments{
\item{report}{The \code{data.frame} generated by \code{pidpos}}

\item{path}{Optional file path to write to.}

\item{include_context}{Binary-flag. If True, adds a \code{Context} column giving a subset
of the sentence being redacted around the token.}
}
\value{
A tibble with columns:
\itemize{
\item \code{If}: The condition (sentence) under which the replacement is to be made.
\item \code{From}: The original token.
\item \code{To}: The replacement token.
}
}
\description{
Convert the output of \code{pidpos} to a \code{tibble} or csv file for editing.
It is intended the user changes the \code{To} column to define desired
redactions.
}
\examples{
data(presidio_text)
example.data <- presidio_text[32:35, ]

# Using regex_factory for illustration; for real PID detection
# the udpipe or spaCy taggers are recommended.
regex_tagger <- regex_factory()
report <- pidpos(example.data, tagger = regex_tagger, filter_func = function(x) x)
report_to_redaction_rules(report)

}
\seealso{
\itemize{
\item To produce the report: \code{\link[=pidpos]{pidpos()}}
\item To automatically replace the \code{To} column: \code{\link[=auto_replace]{auto_replace()}}
\item For redaction: \code{\link[=redact]{redact()}}
}
}
