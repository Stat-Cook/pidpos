% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redact_at_folder.R
\name{redact_at_folder}
\alias{redact_at_folder}
\title{Redact PID across folder structure}
\usage{
redact_at_folder(
  data_path,
  redacter,
  output_path = "Redacted Data",
  extensions = get_implemented_extensions(),
  export_function = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data_path}{The file path at which data is stored}

\item{redacter}{A redaction rules data frame or redaction function}

\item{output_path}{File path to write redacted data to}

\item{extensions}{Optional.  The set of file extensions to scanned for.}

\item{export_function}{A function to define export}

\item{verbose}{Boolean flag - if TRUE will...}
}
\description{
For use as part of the folder level API - this function is the equivalent of
\code{\link[=redact]{redact()}}.  The redacted files are saved in the specified \code{output_path}.
}
\examples{
\dontrun{
input_dir <- withr::local_tempdir()
output_dir <- withr::local_tempdir()

utils::write.csv(
  data.frame(text = "Joey went to London", stringsAsFactors = FALSE),
  file.path(input_dir, "example.csv"),
  row.names = FALSE
)

replace_by <- make_random_replacement()
prepared <- auto_replace(raw_redaction_rules, replacement_func = replace_by)

redact_at_folder(input_dir, redacter = prepared, output_path = output_dir)
}

}
