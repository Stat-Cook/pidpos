# titlecase_comparison

Entity identification tests on the `presidio_text` data set, consisting
of a list of 6 taggers and a basic ensemble method. The 'titlecase'
version is the data mapped to titlecase to improve udpipe catch rate.

## Usage

``` r
titlecase_comparison
```

## Format

An object of class `list` of length 7.
