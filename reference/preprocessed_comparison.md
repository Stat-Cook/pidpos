# preprocessed_comparison

Entity identification tests on the `presidio_text` data set, consisting
of a list of 6 taggers and a basic ensemble method. The 'preprocessed'
version is the data with non XXX character removed.

## Usage

``` r
preprocessed_comparison
```

## Format

An object of class `list` of length 7.
