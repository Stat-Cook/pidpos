# baseline_comparison

Entity identification tests on the `presidio_text` data set, consisting
of a list of 6 taggers and a basic ensemble method. The 'baseline'
version is the data as-is with no preprocessing.

## Usage

``` r
baseline_comparison
```

## Format

An object of class `list` of length 7.
