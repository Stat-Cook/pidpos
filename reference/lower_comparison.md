# lower_comparison

Entity identification tests on the `presidio_text` data set, consisting
of a list of 6 taggers and a basic ensemble method. The 'lower' version
is the data converted to lower case to demonstrate the limitations of
udpipe.

## Usage

``` r
lower_comparison
```

## Format

An object of class `list` of length 7.
